---
import {Formatter} from '@utils/formatter'
const {frontmatter, url} = Astro.props
console.log(frontmatter, url);

---

<div class="lg:flex">
    <img class="object-cover w-full h-56 rounded-lg lg:w-64" 
    src={frontmatter.image} 
    alt={frontmatter.title}>

    <div class="flex flex-col justify-between py-6 lg:mx-6">
        <a href={url} class="text-xl font-semibold  hover:underline text-white ">
            {frontmatter.description}
        </a>
        
        <span class="text-sm text-gray-500 ">{ Formatter.formatDate(frontmatter.date) }</span>
    </div>
</div>

<!-- <div class="lg:flex">
    <img class="object-cover w-full h-56 rounded-lg lg:w-64" 
    src={imgSrc} alt={title}>

    <div class="flex flex-col justify-between py-6 lg:mx-6">
        <a href="#" class="text-xl font-semibold text-gray-800 hover:underline dark:text-white ">
            {title}
        </a>
        
        <span class="text-sm text-gray-500 dark:text-gray-300">{date}</span>
    </div>
</div> -->